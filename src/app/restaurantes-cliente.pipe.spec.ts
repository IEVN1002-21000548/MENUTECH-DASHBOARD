import { RestaurantesClientePipe } from './restaurantes-cliente.pipe';

describe('RestaurantesClientePipe', () => {
  it('create an instance', () => {
    const pipe = new RestaurantesClientePipe();
    expect(pipe).toBeTruthy();
  });
});
